#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{

    int a, sum=0, n, s=0;
    scanf("%d %d", &a, &n);
    for(int i=0; i<n; i++)
    {s=s+a*pow(10,i);//a乘上10的i次方pow函数
        sum=sum+s;}
     printf("%d",sum);


    return 0;
}
