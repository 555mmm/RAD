#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{

    int a, sum=0, n, s1=1;
    scanf("%d", &n);
    for(int i=0; i<n; i++)
    {s1=s1*(i+1);
        sum=sum+s1;}
     printf("%d",sum);


    return 0;
}
